# Server-side OpenFaaS Gateway (internal Kubernetes DNS)
OPENFAAS_GATEWAY_INTERNAL=http://gateway.openfaas.svc.cluster.local:8080

# Development Configuration (for local development)
# OPENFAAS_GATEWAY_INTERNAL=http://localhost:8080

# Client-side configuration (no longer needed for external API)
# API calls now go through SvelteKit API routes
# Legacy configuration (can be removed):
# VITE_API_BASE=https://openfaas.germainleignel.com/function

# Other environment variables
NODE_ENV=development
